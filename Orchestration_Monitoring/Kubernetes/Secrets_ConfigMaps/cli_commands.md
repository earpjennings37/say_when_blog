# 1. Created password file & store it in ….. secrets:
- Generate a file for the secret password file & data

# 2. Create the Nginx Pod:
- vi pod.yaml

# 3. Kubectl exec — curl -u user: <PASSWORD> <IP_ADDRESS>:
- kubectl exec